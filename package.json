{
  "name": "users-manager",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "http-server ./app -a localhost -p 8000 -c-1"
  },
  "author": "Illuminati",
  "license": "ISC",
  "devDependencies": {
    "bower": "^1.8.2",
    "http-server": "^0.10.0"
  }
}
